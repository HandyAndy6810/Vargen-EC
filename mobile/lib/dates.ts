// Date helpers live in shared/mobile-types.ts so the server can enforce the
// same rules; re-exported here for convenient '@/lib/dates' imports.
export { isValidISODate, toISODate } from '@shared/mobile-types';
